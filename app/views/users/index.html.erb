<ul class="list-group">
	<% @users.each do |user| %>
  	<li class="list-group-item">
    	<%= link_to user.full_name, user %>

    	<span class="badge"><%= user.posts.published.count %> adet yazısı var.</span>
  	</li>
  <% end %>
</ul>